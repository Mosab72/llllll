# دليل البدء السريع ⚡

## خطوات التشغيل في 3 دقائق

### 1️⃣ تحميل الملفات

قم بتحميل جميع الملفات التالية:

```
📁 المشروع/
├── 📄 index.html          ← الصفحة الرئيسية
├── 🎨 styles.css          ← التنسيقات
├── ⚙️ script.js           ← البرمجة
├── 💾 data.js             ← البيانات (20 عقد نموذجي)
├── 📋 data_template.js    ← قالب البيانات
├── 📖 README.md           ← الدليل الكامل
└── 📝 LICENSE             ← الترخيص
```

### 2️⃣ افتح المشروع

**الطريقة الأولى (بسيطة):**
- افتح ملف `index.html` مباشرة في المتصفح
- ✅ لا حاجة لأي إعدادات!

**الطريقة الثانية (خادم محلي):**
```bash
# افتح Terminal في مجلد المشروع

# باستخدام Python
python -m http.server 8000

# أو Node.js
npx http-server -p 8000

# ثم افتح: http://localhost:8000
```

### 3️⃣ إضافة بياناتك

1. افتح ملف `data.js`
2. أضف عقودك بنفس البنية:

```javascript
{
  id: "021",  // معرف فريد
  program: "اسم البرنامج",
  college: "الكلية",
  university: "الجامعة",
  degree: "الدرجة",
  department: "الإدارة",
  status: "تحت الإجراء",
  progress: "40%",
  docReceiveStatus: "تم التسليم",
  docReceiveDate: "01/15/25",
  startDate: "03/07/24",
  endDate: "03/06/25"
  // ... المزيد من الحقول
}
```

3. احفظ الملف
4. أعد تحميل الصفحة (F5)

---

## 🎯 الميزات الرئيسية

### 📊 لوحة التحكم
- إحصائيات شاملة
- رسوم بيانية تفاعلية
- معلومات فورية

### 🔍 البحث والفلترة
- بحث نصي سريع
- فلترة حسب الحالة
- فلترة حسب التاريخ

### 📁 علامات التبويب (7 أقسام)
1. لوحة التحكم
2. حالة العقود
3. الجامعات
4. التخصصات
5. التواريخ
6. انتهاء العقود
7. الإدارات المختصة

---

## 💡 نصائح سريعة

### ✅ افعل:
- استخدم معرّف (id) فريد لكل عقد
- حافظ على تنسيق التواريخ (MM/DD/YY)
- تأكد من إغلاق الأقواس والفواصل

### ❌ لا تفعل:
- لا تنسَ الفاصلة بين العقود
- لا تضع فاصلة بعد آخر عقد
- لا تغير أسماء الحقول الأساسية

---

## 🐛 حل المشاكل السريع

### المشكلة: لا تظهر البيانات
✅ تأكد من:
- وجود ملف `data.js` في نفس المجلد
- صحة بنية JSON
- فتح Console للتحقق من الأخطاء (F12)

### المشكلة: الرسوم البيانية لا تعمل
✅ تأكد من:
- اتصال الإنترنت (لتحميل Chart.js)
- عدم حظر المتصفح للسكريبتات الخارجية

### المشكلة: لا يعمل البحث
✅ تأكد من:
- تحميل ملف `script.js` بشكل صحيح
- عدم وجود أخطاء في Console

---

## 📱 الاستخدام على الهاتف

1. افتح المتصفح على الهاتف
2. انتقل إلى الموقع المحلي أو رفعه أونلاين
3. جميع الميزات تعمل بشكل كامل!

---

## 🚀 رفع المشروع أونلاين

### GitHub Pages (مجاني):
```bash
# 1. أنشئ مستودع على GitHub
# 2. ارفع الملفات
git init
git add .
git commit -m "Initial commit"
git remote add origin YOUR_REPO_URL
git push -u origin main

# 3. فعّل GitHub Pages من الإعدادات
# 4. زر الموقع: https://username.github.io/repo-name
```

### Netlify (مجاني):
1. سجّل في Netlify.com
2. اسحب المجلد إلى Netlify
3. انشر! ✨

### Vercel (مجاني):
1. سجّل في Vercel.com
2. استورد من GitHub
3. انشر تلقائياً!

---

## 📊 إضافة البيانات الكاملة (445 عقد)

### الطريقة اليدوية:
1. افتح ملف `data.js`
2. انسخ بنية العقد من `data_template.js`
3. أضف جميع عقودك
4. احفظ

### الطريقة التلقائية:
1. استخدم `process_data.py`
2. عدّل السكريبت ليقرأ من Excel/CSV
3. شغّله: `python process_data.py`
4. سيُنشئ `data.js` تلقائياً

---

## 🎨 التخصيص السريع

### تغيير الألوان:
افتح `styles.css` وعدّل:
```css
:root {
  --primary-color: #2563eb;  /* أزرق */
  --accent-color: #f59e0b;   /* برتقالي */
}
```

### تغيير العنوان:
افتح `index.html` وعدّل:
```html
<title>عنوانك هنا</title>
<h1>عنوانك هنا</h1>
```

---

## 🆘 الدعم

**لديك سؤال؟**
- 📖 اقرأ `README.md` الكامل
- 🐛 افتح Console (F12) للأخطاء
- 💬 أنشئ Issue على GitHub

---

## ✨ البدء الآن!

```bash
# 1. افتح Terminal
cd path/to/project

# 2. شغّل خادم محلي
python -m http.server 8000

# 3. افتح المتصفح
# http://localhost:8000

# 4. استمتع! 🎉
```

---

**تهانينا! 🎊**  
أنت الآن جاهز لاستخدام نظام تحليل عقود الاعتماد الأكاديمي!

**⏱️ الوقت المستغرق:** 3 دقائق  
**🔧 المتطلبات:** متصفح حديث فقط  
**💰 التكلفة:** مجاني 100%  
**📱 التوافق:** جميع الأجهزة

---

Made with ❤️ | [المزيد في README.md](./README.md)